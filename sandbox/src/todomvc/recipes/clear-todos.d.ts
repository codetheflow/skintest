import { StorySchema } from '@skintest/sdk';
export declare const clear_todos: import("@skintest/sdk").ClientRecipe<(this: import("@skintest/sdk").Client) => Promise<{
    message: string;
    plan: StorySchema;
}>>;
